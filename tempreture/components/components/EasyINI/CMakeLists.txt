set(COMPONENT_SRCDIRS
    "src"
)

set(COMPONENT_ADD_INCLUDEDIRS
    "src"
)

set(COMPONENT_REQUIRES
   "arduino"
)

register_component()
# idf_component_register(SRCS ${COMPONENT_SRCDIRS} INCLUDE_DIRS ${COMPONENT_ADD_INCLUDEDIRS} REQUIRES ${COMPONENT_REQURES})
